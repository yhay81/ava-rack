# Security

Report vulnerabilities through GitHub private vulnerability reporting.

The service applies a restrictive Content Security Policy, rejects cross-site telemetry, validates the entire event schema, rate-limits writes, hashes random client identifiers, and retains only allowlisted aggregate events.

Do not include credentials or live exploit details in a public issue.
